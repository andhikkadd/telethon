# Telethon API Configuration
# Get these from https://my.telegram.org
API_ID=
API_HASH=

# Session File Configuration
SESSION_NAME=sessions/promo_userbot

# Master / Administrator Authorization
# Master ID takes priority. If MASTER_ID is empty/not set, MASTER_USERNAME is checked.
MASTER_USERNAME=your_telegram_username
MASTER_ID=

# Reporting and Backup Settings
REPORT_TARGET=me
BACKUP_TARGET=me
BACKUP_PASSWORD=your_secure_backup_password
SEND_REPORT=true
RUN_WAVE_ON_START=false

# Auto-Scheduling Wave Settings (Delays in minutes)
DEFAULT_MIN_DELAY_MINUTES=60
DEFAULT_MAX_DELAY_MINUTES=180
DELAY_BETWEEN_GROUPS_SECONDS=15

# Backup Security Settings
# If set to true, it will allow creating normal zip file if gpg is not installed.
ALLOW_UNENCRYPTED_BACKUP=false
BACKUP_INCLUDE_ENV=false
DELETE_LOCAL_BACKUP_AFTER_SEND=true

# System Configuration
DATABASE_PATH=data/bot.db
LOG_LEVEL=INFO

# FastAPI Web Control Panel Settings
ENABLE_WEB_PANEL=true
WEB_HOST=127.0.0.1
WEB_PORT=8000
WEB_ADMIN_USERNAME=admin
WEB_ADMIN_PASSWORD=
WEB_SESSION_SECRET=
WEB_TRUST_PROXY=false
